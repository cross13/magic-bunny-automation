/// <reference types="Cypress" />

// Refactor using commands

describe('Login into the app using a command', () => {

    // Visitar nuestra webapp utilizando un hook

    it('should fail login with wrong credentials & check the error msg', () => {

    });

    it('login into the magic hat app & check the correct url "/magic" ', () => {

    });

    it('login into the magic hat app & check the correct url "/magic" & logout', () => {

    });

});