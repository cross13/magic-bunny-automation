/// <reference types="Cypress" />

context('Authenticate into the app', () => {
    beforeEach(() => {
      cy.visit('/');
    })

    it('Should render the correct login page', () => {

    });

});