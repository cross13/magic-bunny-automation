/// <reference types="Cypress" />

describe('Bypass Login', () => {

    // Visitar nuestra webapp utilizando un hook
    // Limpiar el locaStorage de nuestro usuario

    it('should avoid the login page using the api', () => {

        // Loguearse consultado directamente el endpoint de auth 
        // Setear correctamente el localStorage

    });

});